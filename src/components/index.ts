/**
 * Component Exports
 * 
 * Barrel file for all components.
 * 
 * Structure:
 * - ui/       → Atomic/primitive components (Button, Card, etc.)
 * - layout/   → Navbar, Footer (always rendered)
 * - sections/ → Page building blocks (HeroSection, etc.)
 * - content/  → MDX rendering components
 */

// UI primitives
export * from "./ui";

// Layout components
export * from "./layout";

// Section components
export * from "./sections";

// Content components
export * from "./content";

